/*
import { TestBed } from '@angular/core/testing';

import { Material2AppAppComponent,DialogContent } from './app.component';

describe('App', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({ declarations: [Material2AppAppComponent]});
  });

  it ('should work', () => {
    let fixture = TestBed.createComponent(Material2AppAppComponent);
    expect(fixture.componentInstance instanceof Material2AppAppComponent).toBe(true, 'should create AppComponent');
  });
});
*/